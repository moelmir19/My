{# views/home/products-list.twig #}

<section id="block{{ id }}" class="ui-product-section">
    {% if title %}
        <div class="section_title ui-section-heading">
            <h3 class="display-4 text-black bg-transparent">{{ title }}</h3>
            <p class="section-description">
                {{ content['settings']['producrs_description'] }}
            </p>
        </div>
    {% endif %}

    <div id="{{ id }}" class="mb-20">
        {% if type == 'slider' %}
            {% include 'home/products-slider.twig' %}
        {% else %}
            <div class="product-grid-4">
                {% for product in products %}
                    <article class="product-box ui-product-card">
                        <div class="product-card">
                            {% if product['new'] %}
                                <div class="sticker new">{{ ('app.products.new'|trans) }}</div>
                            {% endif %}
                            <a href="/{{ product['url_key'] }}" title="{{ product['name'] }}" class="product-a ui-product-card__link">
                                <div class="product-image ui-product-card__media">
                                    <img src="{{ product.product.base_image_url }}" alt="{{ product['name'] }}" loading="lazy"/>
                                    {% if product['promotion_title'] %}<div class="card-promo">{{ product['promotion_title'] }}</div>{% endif %}
                                </div>
                                <div class="product-information ui-product-card__body">
                                    <div class="product-name"><p>{{ product['name'] }}</p></div>
                                </div>
                            </a>
                            <div class="product-bottom-row ui-product-card__footer">
                                <div class="ui-product-card__price">{{ product.html_price | raw }}</div>
                                <div class="hover-buttons ui-product-card__quick-actions">
                                    <form action="/customer/wishlist/add/{{ product.product_id }}" method="GET">
                                        <input type="hidden" name="product_id" value="{{ product.product_id }}">
                                        <button type="submit" class="border-0 btn" title="إضافة منتج إلى قائمة الأمنيات"><i class="icon-heart"></i></button>
                                    </form>
                                    <form id="addToCart-{{ product.product_id }}">
                                        <input type="hidden" name="_token" value="{{ csrf_token() }}">
                                        <input type="hidden" name="product_id" value="{{ product.product_id }}">
                                        <input type="hidden" name="quantity" value="1">
                                        {% set outOfStock = getConfig('config.products.out-of-stock') == '1' %}
                                        {% if outOfStock and not product.saleable %}
                                            <span class="not-available">{{ ('app.products.out_of_stock'|trans) }}</span>
                                        {% else %}
                                            <button type="button" class="btn addToCart border-0" {{ product.saleable ? '' : 'disabled' }} data-id="{{ product.product_id }}"><i class="icon-cart"></i></button>
                                        {% endif %}
                                    </form>
                                </div>
                            </div>
                        </div>
                    </article>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</section>